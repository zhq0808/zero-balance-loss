# 🔐 分布式锁可视化方案

## 🎯 设计目标

设计一套专门针对分布式锁的可视化系统,直观展示:
1. **多实例部署**场景下的锁竞争
2. **Redis 作为中心协调者**的作用
3. **锁的获取、持有、释放**全生命周期
4. **锁超时、续期、死锁**等异常场景

---

## 📐 可视化架构

### 布局设计：五列架构

```
┌─────────────┬─────────────┬─────────────┬─────────────┬─────────────┐
│  实例 1     │  实例 2     │   Redis     │  实例 3     │   数据库    │
│  (App-1)    │  (App-2)    │  (协调者)   │  (App-3)    │  (MySQL)    │
├─────────────┼─────────────┼─────────────┼─────────────┼─────────────┤
│             │             │             │             │             │
│  请求队列   │  请求队列   │  锁状态表   │  请求队列   │  余额显示   │
│             │             │             │             │             │
│  🔵 Req-1   │  🟡 Req-3   │  🔒 Lock    │  🟢 Req-5   │  💰 1000    │
│  🔵 Req-2   │  🟡 Req-4   │  Holder:    │  🟢 Req-6   │             │
│             │             │  App-1      │             │             │
│  状态:等待  │  状态:等待  │  TTL: 8.5s  │  状态:等待  │  版本: v3   │
│             │             │             │             │             │
└─────────────┴─────────────┴─────────────┴─────────────┴─────────────┘
        ↓              ↓              ↑              ↓              ↑
        └──────────────┴──────────────┴──────────────┴──────────────┘
                        并发竞争 Redis 分布式锁
```

---

## 🎨 核心可视化元素

### 1️⃣ **服务实例卡片**

```html
┌─────────────────────────────┐
│  🖥️  实例 1 (App-1)          │
│  Status: 🟢 Running         │
│  Port: 8081                 │
├─────────────────────────────┤
│  📋 请求队列 (3)             │
│                             │
│  🔵 Req-001                 │
│  ⏳ 等待锁...               │
│  ⏱️ 等待时间: 120ms         │
│                             │
│  🔵 Req-002                 │
│  ✅ 持有锁                  │
│  🔒 剩余时间: 8.5s          │
│  📊 执行进度: ████░░░ 60%   │
│                             │
│  🔵 Req-003                 │
│  😴 队列中                  │
└─────────────────────────────┘
```

**设计要点:**
- 实例卡片颜色区分 (实例1=蓝色, 实例2=黄色, 实例3=绿色)
- 显示请求队列深度
- 实时显示锁等待时间
- 进度条显示请求执行进度

---

### 2️⃣ **Redis 锁协调器**

```html
┌─────────────────────────────┐
│  🔐 Redis 分布式锁状态       │
│  Lock Key: lock:account:1   │
├─────────────────────────────┤
│  当前持有者: 🖥️ App-1       │
│  锁值: uuid-abc-123         │
│  剩余TTL: ████████░░ 8.5s   │
│                             │
│  ⏳ 等待队列 (5个请求)       │
│  ┌───────────────────────┐ │
│  │ 🟡 App-2-Req-003      │ │
│  │ 等待: 1.2s            │ │
│  ├───────────────────────┤ │
│  │ 🟢 App-3-Req-001      │ │
│  │ 等待: 0.8s            │ │
│  ├───────────────────────┤ │
│  │ 🔵 App-1-Req-004      │ │
│  │ 等待: 0.3s            │ │
│  └───────────────────────┘ │
│                             │
│  📊 统计                    │
│  获取成功: 23次             │
│  获取失败: 5次              │
│  平均等待: 85ms             │
└─────────────────────────────┘
```

**设计要点:**
- 显示当前锁持有者
- TTL 倒计时进度条 (动态更新)
- 等待队列可视化
- 统计信息 (成功率、平均等待时间)

---

### 3️⃣ **锁生命周期时间线**

```
时间线 (横向滚动,纳秒级精度)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔵 App-1-Req-001:
   ├─ 🔍 尝试获取锁 (t=0ms)
   ├─ ✅ 获取成功 (t=5ms)
   ├─ 📖 读取数据 (t=10ms)
   ├─ 🧮 计算 (t=15-25ms)
   ├─ ✍️ 写入数据 (t=30ms)
   └─ 🔓 释放锁 (t=35ms)

🟡 App-2-Req-003:
   ├─ 🔍 尝试获取锁 (t=3ms)
   ├─ ⏳ 等待中... (t=3-35ms) ← 等待 32ms
   ├─ ✅ 获取成功 (t=35ms)
   ├─ 📖 读取数据 (t=40ms)
   └─ ... (执行中)

🟢 App-3-Req-005:
   ├─ 🔍 尝试获取锁 (t=8ms)
   └─ ⏳ 等待中... (t=8ms-now)
```

**设计要点:**
- 横向时间轴
- 不同颜色表示不同阶段
- 清晰标注等待时间
- 自动滚动跟随最新事件

---

### 4️⃣ **锁竞争热力图**

```html
实时竞争强度 (每秒更新)
┌─────────────────────────────┐
│  🔥 锁竞争热力图             │
│                             │
│  23:10:00 ████████ 8/10     │
│  23:10:01 █████░░░ 5/10     │
│  23:10:02 ██████░░ 6/10     │
│  23:10:03 ██████████ 10/10  │ ← 高竞争!
│  23:10:04 ███░░░░░ 3/10     │
│  23:10:05 ████████░ 8/10    │
│                             │
│  颜色图例:                  │
│  🟢 0-3   低竞争            │
│  🟡 4-6   中等竞争          │
│  🔴 7-10  高竞争            │
└─────────────────────────────┘
```

---

### 5️⃣ **异常场景可视化**

#### 场景1: 锁超时
```
🚨 锁超时场景
─────────────────────────────
🔵 App-1-Req-001:
   ├─ ✅ 获取锁 (TTL=10s)
   ├─ 🐢 执行缓慢... (耗时 12s)
   └─ ⚠️ 锁已过期! (t=10s)

🟡 App-2-Req-003:
   ├─ ⏳ 等待 10s...
   ├─ ✅ 获取锁 (App-1超时)
   └─ 📖 读取数据

结果: ❌ 并发冲突! (App-1 和 App-2 同时操作数据)
```

#### 场景2: 锁续期
```
🔄 锁续期场景
─────────────────────────────
🔵 App-1-Req-001:
   ├─ ✅ 获取锁 (TTL=10s)
   ├─ 🔄 自动续期 (t=8s, 新TTL=10s)
   ├─ 🔄 自动续期 (t=16s, 新TTL=10s)
   ├─ ✅ 执行完成 (t=18s)
   └─ 🔓 释放锁

看门狗 (Watchdog):
   📡 每 8s 检查一次
   ✓ 如果请求还在执行 → 续期
   ✗ 如果请求已完成 → 不续期
```

---

## 🛠️ 技术实现方案

### 前端架构

```javascript
// 1. WebSocket 实时通信
const ws = new WebSocket('ws://localhost:8080/ws/lock-monitor');

ws.onmessage = (event) => {
    const data = JSON.parse(event.data);
    
    switch(data.type) {
        case 'lock_acquired':
            updateLockStatus(data);
            animateLockAcquire(data);
            break;
        case 'lock_released':
            animateLockRelease(data);
            break;
        case 'lock_waiting':
            updateWaitingQueue(data);
            break;
        case 'lock_timeout':
            showTimeoutWarning(data);
            break;
    }
};

// 2. 实时 TTL 倒计时
function startTTLCountdown(lockId, ttlSeconds) {
    const progressBar = document.getElementById(`ttl-${lockId}`);
    const interval = setInterval(() => {
        ttlSeconds -= 0.1;
        const percentage = (ttlSeconds / 10) * 100;
        progressBar.style.width = percentage + '%';
        
        if (ttlSeconds <= 0) {
            clearInterval(interval);
            showLockExpired(lockId);
        }
    }, 100);
}

// 3. 锁竞争动画
function animateLockCompetition(requestA, requestB) {
    // 两个请求同时向 Redis 发起竞争
    createRacingAnimation(requestA, requestB);
    
    // 只有一个获胜
    setTimeout(() => {
        highlightWinner(requestA);
        showLoserWaiting(requestB);
    }, 500);
}
```

### 后端 API 设计

```go
// 1. WebSocket 端点
GET /ws/lock-monitor
→ 推送锁状态实时更新

// 2. 获取当前锁状态
GET /api/lock/status
{
    "lock_key": "lock:account:1",
    "holder": "app-1-uuid-123",
    "ttl": 8.5,
    "waiting_queue": [
        {
            "instance": "app-2",
            "request_id": "req-003",
            "wait_time": 1200
        }
    ],
    "stats": {
        "total_acquired": 23,
        "total_failed": 5,
        "avg_wait_time": 85
    }
}

// 3. 获取所有实例状态
GET /api/instances
[
    {
        "id": "app-1",
        "port": 8081,
        "status": "running",
        "active_requests": 3,
        "holding_lock": true
    },
    {
        "id": "app-2",
        "port": 8082,
        "status": "running",
        "active_requests": 2,
        "holding_lock": false
    }
]

// 4. 锁事件历史
GET /api/lock/history?limit=50
[
    {
        "timestamp": "2026-02-04T23:10:05.123Z",
        "event": "acquired",
        "instance": "app-1",
        "request_id": "req-001",
        "wait_time": 12
    },
    {
        "timestamp": "2026-02-04T23:10:05.158Z",
        "event": "released",
        "instance": "app-1",
        "request_id": "req-001",
        "hold_time": 35
    }
]
```

---

## 🎬 交互功能

### 1. 手动触发并发场景
```javascript
// 控制面板
<button onclick="triggerConcurrentAttack(3)">
    🚀 发起3实例并发攻击
</button>

// 效果: 同时向 3 个服务实例发送请求,观察锁竞争
```

### 2. 模拟异常场景
```javascript
// 测试按钮组
<button onclick="simulateLockTimeout()">
    ⏰ 模拟锁超时
</button>

<button onclick="simulateInstanceCrash('app-2')">
    💥 模拟实例崩溃
</button>

<button onclick="simulateRedisDown()">
    🔌 模拟Redis宕机
</button>
```

### 3. 对比模式切换
```javascript
// 三列对比布局
┌─────────────┬─────────────┬─────────────┐
│  无锁模式   │  单机锁     │  分布式锁   │
│  (Lost      │  (Mutex)    │  (Redis)    │
│  Update)    │             │             │
├─────────────┼─────────────┼─────────────┤
│ ❌ 数据丢失 │ ⚠️ 单实例OK │ ✅ 多实例OK │
│ 10000→9500  │ 10000→9000  │ 10000→9000  │
│ 丢失: 500   │ 丢失: 0     │ 丢失: 0     │
└─────────────┴─────────────┴─────────────┘

<button onclick="switchComparisonMode()">
    🔄 切换对比模式
</button>
```

---

## 📊 性能指标可视化

### 实时仪表盘

```html
┌────────────────────────────────────────┐
│  📈 分布式锁性能仪表盘                  │
├────────────────────────────────────────┤
│  QPS: 850 req/s          ████████░░    │
│  锁获取成功率: 98.5%     ██████████    │
│  平均等待时间: 12ms      ███░░░░░░░    │
│  P99等待时间: 85ms       ██████░░░░    │
│  锁持有时长: 25ms        ████░░░░░░    │
└────────────────────────────────────────┘
```

### 性能对比图表
```javascript
// Chart.js 折线图
const chart = new Chart(ctx, {
    type: 'line',
    data: {
        labels: ['无锁', '单机锁', 'Redis锁', '乐观锁'],
        datasets: [{
            label: 'TPS',
            data: [5000, 1000, 800, 2000],
            borderColor: '#667eea'
        }, {
            label: 'P99延迟(ms)',
            data: [10, 50, 80, 30],
            borderColor: '#f59e0b'
        }]
    }
});
```

---

## 🎯 实现优先级

### 第一阶段 (核心功能)
- [x] 五列布局 (3实例 + Redis + DB)
- [x] 锁状态实时显示
- [x] TTL 倒计时进度条
- [x] 等待队列可视化

### 第二阶段 (增强功能)
- [ ] 锁生命周期时间线
- [ ] WebSocket 实时推送
- [ ] 手动触发并发场景
- [ ] 锁竞争热力图

### 第三阶段 (高级功能)
- [ ] 异常场景模拟
- [ ] 性能对比模式
- [ ] 看门狗续期可视化
- [ ] 分布式追踪集成

---

## 🎨 UI 设计图

### 布局草图
```
┌──────────────────────────────────────────────────────────────────┐
│  🔐 分布式锁可视化器 - Distributed Lock Visualizer              │
└──────────────────────────────────────────────────────────────────┘
┌──────────────────────────────────────────────────────────────────┐
│  [🚀 发起并发攻击] [⏰ 模拟超时] [💥 实例崩溃] [🔄 重置]        │
│  模式: ● 分布式锁  ○ 单机锁  ○ 无锁                             │
└──────────────────────────────────────────────────────────────────┘

┌──────────┬──────────┬──────────┬──────────┬──────────┐
│🖥️ App-1  │🖥️ App-2  │🔐 Redis  │🖥️ App-3  │💾 MySQL  │
│Port:8081 │Port:8082 │Port:6379 │Port:8083 │Port:3306 │
│🟢 Running│🟢 Running│🟢 Running│🟢 Running│🟢 Running│
├──────────┼──────────┼──────────┼──────────┼──────────┤
│          │          │          │          │          │
│📋 队列(2)│📋 队列(3)│🔒 当前锁 │📋 队列(1)│💰 余额   │
│          │          │Holder:   │          │   9850   │
│🔵 Req-01 │🟡 Req-03 │App-1     │🟢 Req-06 │          │
│✅ 持有锁 │⏳ 等待锁 │          │😴 队列中 │版本: v5  │
│⏱️ 8.5s  │⏱️ 1.2s  │TTL:      │          │          │
│████████░░│          │████████░░│          │          │
│          │🟡 Req-04 │8.5s      │          │          │
│🔵 Req-02 │⏳ 等待锁 │          │          │          │
│😴 队列中 │⏱️ 0.8s  │⏳ 等待队列│          │          │
│          │          │┌────────┐│          │          │
│          │🟡 Req-05 ││Req-03  ││          │          │
│          │😴 队列中 ││Req-04  ││          │          │
│          │          ││Req-02  ││          │          │
│          │          │└────────┘│          │          │
└──────────┴──────────┴──────────┴──────────┴──────────┘

┌──────────────────────────────────────────────────────────────────┐
│  ⏱️ 锁生命周期时间线 (最近 10 个事件)                            │
├──────────────────────────────────────────────────────────────────┤
│  🔵 App-1-Req-01: ✅获取(t=0) → 📖读(t=5) → ✍️写(t=20) → 🔓释放(t=25)│
│  🟡 App-2-Req-03: 🔍尝试(t=3) → ⏳等待22ms → ✅获取(t=25) → ...   │
│  🟢 App-3-Req-06: 🔍尝试(t=8) → ⏳等待中...                      │
└──────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│  📊 性能统计 (实时更新)                                          │
├────────────┬────────────┬────────────┬────────────┬──────────────┤
│ QPS: 850   │ 成功率:98% │ 平均等待:  │ P99等待:   │ 锁持有时长:  │
│ ████████░░ │ ██████████ │ 12ms       │ 85ms       │ 25ms         │
└────────────┴────────────┴────────────┴────────────┴──────────────┘
```

---

## 🚀 快速开始

### 1. 启动多实例环境
```bash
docker-compose up -d
# 启动 3 个应用实例 + Redis + MySQL + Nginx
```

### 2. 访问可视化页面
```
http://localhost:8080/distributed-lock-visualizer.html
```

### 3. 发起并发测试
```javascript
// 点击 "发起并发攻击" 按钮
// 或使用 API:
curl -X POST http://localhost:8080/api/concurrent-attack \
  -d '{"instances": 3, "requests_per_instance": 10}'
```

---

## 💡 教学价值

通过这个可视化系统,学习者可以直观看到:

1. **分布式锁的必要性**
   - 单机锁在多实例下完全失效
   - Redis 锁能跨进程协调

2. **锁竞争的真实场景**
   - 多个请求同时尝试获取锁
   - 等待队列的形成和消化

3. **性能代价**
   - 分布式锁的延迟开销
   - 与乐观锁的性能对比

4. **异常处理**
   - 锁超时的危险性
   - 看门狗续期的重要性
   - Redis 宕机的应对方案

---

**下一步**: 是否需要我开始实现这个可视化页面的 HTML/CSS/JS 代码?
